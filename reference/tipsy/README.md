# TIPSY Reference Corpus

This directory holds tracked reference material copied or extracted from the
installed `C:\Program Files\TIPSY 4.7\` product tree for FEMIC reverse-
engineering work.

Current contents:

- packaged PDF references already staged locally;
- fully extracted HTML Help trees under `chm_extracted/`.

These files are reference artifacts, not canonical FEMIC runtime inputs.
They are checked in so future BTC/TIPSY/FANSIER reverse-engineering work can
grep, diff, and cite the installed help/documentation corpus without needing to
repeat local extraction steps.
